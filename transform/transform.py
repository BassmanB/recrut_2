from .config import config_sources
from os import path

"""
class Transform:

    def __init__(self, df, full_path):
        self.df = df
        self.full_path = full_path
"""


#class Transform:



"""
def classify_source(full_path):
    for src_conf in config_sources:
        if config_sources[src_conf]["src_dir"] in path.split(full_path)[0]:
            return src_conf
        return False
"""

